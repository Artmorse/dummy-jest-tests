name: Example Workflow 2

on: [push]

jobs:
  first-job:
    runs-on: ubuntu-latest
    outputs:
      myOutput: ${{ steps.set-output.outputs.pr_title }}
    steps:
      - name: Set an output value
        id: set-output
        run: echo "pr_title=Hello World" >> $GITHUB_OUTPUT

  second-job:
    runs-on: ubuntu-latest
    needs: first-job
    steps:
      - name: Use output from first-job
        run: |
          echo "The value passed from first-job is: ${{ needs.first-job.outputs.myOutput }}"
